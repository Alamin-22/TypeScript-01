{
  // Types in Functions

  const MultiplyFunc = (value1: number, value2: number): number => {
    return value1 * value2;
  };

  const Result1 = MultiplyFunc(5, 5); // => as we already declared type for the arguments we cant provide any type of data we just have to provide only Number on that Function as an arguments
}
